package application

import (
	"{{.ProjectName}}/internal/shared/config"
	"{{.ProjectName}}/internal/shared/logger"
)

type {{.ContextName}}ServiceAPI interface {
}

type {{.ContextNameCamel}}ServiceAPIImpl struct {
	logger          logger.Logger
	config          config.AppConfig
}

func New{{.ContextName}}ServiceAPI(
	logger logger.Logger,
	config config.AppConfig,
) {{.ContextName}}ServiceAPI {
	return &{{.ContextNameCamel}}ServiceAPIImpl{
		logger:          logger,
		config:          config,
	}
}