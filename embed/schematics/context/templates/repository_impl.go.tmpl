package infrastructure

import (
	"{{ .Module }}/internal/{{ .ContextName | lower }}/domain"
	"{{ .Module }}/internal/shared/logger"

	"gorm.io/gorm"
)

type {{ .ContextName | lower }}Repository struct {
	db     *gorm.DB
	logger logger.Logger
}

func New{{ .ContextName | title }}Repository(db *gorm.DB, logger logger.Logger) domain.{{ .ContextName | title }}Repository {
	return &{{ .ContextName | lower }}Repository{db: db, logger: logger}
}
